# run the following to get the empirical spectrum of the lsa/log_count/ppmi matrices
# record the .pkl file output
python LSA.py

# run the following to get the estimated noise level
# record \sigma, the output
python noise_est_pmi.py

# run the following to cosine similarity
python cos.py
